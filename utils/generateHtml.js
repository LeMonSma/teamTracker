const Employee = require("../lib/Employee")


function managerHtml(data) {
    // return console.log(`
    // ${data.name}
    // ${data.email}
    // ${data.employeeId}`)
    // return special categories for each worker
}

module.exports = generateHtml